import React from "react"
import { Switch as SwitchElement } from "../ui/switch"
import { SwitchProps } from "../../types"

export const Switch = (props: SwitchProps) => {
  return <SwitchElement {...props} />
}

export default Switch